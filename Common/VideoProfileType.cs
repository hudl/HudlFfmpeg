namespace Hudl.Ffmpeg.Common
{
    /// <summary>
    /// enumaration containing the known and extended profiles for ffmpeg
    /// </summary>
    /// <remarks></remarks>
    public enum VideoProfileType
    {
        /// <summary>
        /// indicates that the output video profile should be Baseline (for maximized compatibility)
        /// </summary>
        Baseline,
        /// <summary>
        /// indicates that the output video profile should be High
        /// </summary>
        High
    }
}
